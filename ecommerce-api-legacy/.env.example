# Copy to .env and fill in real values for production. Never commit .env.
PORT=3000
DB_PATH=:memory:
DB_USER=
DB_PASS=
PAYMENT_GATEWAY_KEY=
SMTP_USER=
# Optional — when set, /api/admin/* and DELETE /api/users/:id require this
# value in the x-admin-api-key request header.
ADMIN_API_KEY=
